<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

	<camelContext xmlns="http://camel.apache.org/schema/spring">
		<!-- scan for RouteBuilder classes in the package -->
		<package>org.jeets.georouter</package>
	</camelContext>

	<bean id="jmsConnectionFactory" class="org.apache.activemq.spring.ActiveMQConnectionFactory">
		<!-- expose an in-JVM transport between AMQ and Camel on server 
			comment to run against localhost -->
		<property name="brokerURL" value="vm://localhost?broker.persistent=false" />
		<property name="trustAllPackages" value="true" />
		<!-- System Entities <property name="trustedPackages"> 
		<list> <value>org.jeets.model.traccar.jpa</value> </list> </property> -->
	</bean>
	<bean id="jmsConfig" class="org.apache.camel.component.jms.JmsConfiguration">
		<property name="connectionFactory" ref="jmsConnectionFactory" />
		<property name="concurrentConsumers" value="5" />
	</bean>
	<bean id="activemq" class="org.apache.activemq.camel.component.ActiveMQComponent">
		<property name="configuration" ref="jmsConfig" />
		<!-- expose a TCP transport for clients to use -->
		<property name="brokerURL" value="tcp://localhost:61616" />
		<!-- TODO: "tcp://localhost:${tcp.port}" -->
	</bean>

</beans> 
