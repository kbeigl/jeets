package de.jeets.traccar.dao;

import java.util.List;

import org.hibernate.Session;

import de.jeets.model.traccar.hibernate.Event;
import de.jeets.model.traccar.hibernate.Position;

public class HibernateDao {

	public static List<Event> getEventsEntities() {
		Session session = HibernateUtil.currentSession();
//		session.beginTransaction();
		List<Event> result = session.createQuery("from Events").list();
//		session.flush();
//		session.getTransaction().commit();
//		session.close();
		HibernateUtil.closeSession();
		return result;
	}

	public static List<Position> getPositionsEntities() {
		Session session = HibernateUtil.currentSession();

		List<Position> result = null;
		result = session.createQuery("from Position").list();
//		Query query = session.createQuery("from Positions where protocol = :protocol ");
//		query.setParameter("protocol", "gps103");
//		result = query.list();
		
		HibernateUtil.closeSession();
		return result;
	}

}
